export const getInitialize = (state) => {
    return state.app.initialize;
}

export const getAvatar = (state) => {
    return state.app.defaultAvatar;
}

export const getPanorama = (state) => {
    return state.app.defaultPanorama;
}